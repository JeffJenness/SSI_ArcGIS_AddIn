<controls:ProWindow x:Class="SSI_ArcGIS_Addin.QueryBuilderWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
                    xmlns:qb="clr-namespace:ArcGIS.Desktop.Mapping.Controls;assembly=ArcGIS.Desktop.Mapping"
                    Title="Build Query"
                    Width="660" Height="500" MinWidth="500" MinHeight="380"
                    WindowStartupLocation="CenterOwner"
                    ResizeMode="CanResize" ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap" />

        <qb:QueryBuilderControl x:Name="QbControl" Grid.Row="1" />

        <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,10,0,0">
            <Button DockPanel.Dock="Right" Content="Cancel" MinWidth="80" Padding="10,3" Margin="8,0,0,0"
                    IsCancel="True" Click="OnCancelClick" />
            <Button DockPanel.Dock="Right" Content="OK" MinWidth="80" Padding="10,3"
                    IsDefault="True" Click="OnOkClick" />
        </DockPanel>
    </Grid>
</controls:ProWindow>
